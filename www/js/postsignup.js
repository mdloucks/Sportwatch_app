


function postSignupPage() {
    // initialize the home page which will load the nav and other things but no content
    let home = new homePage();

    $("#app").html(`Welcome to sportwatch for your first time!`);
}